apiVersion: v1
kind: ConfigMap
metadata:
  name: kustomization-config
  namespace: cloud-cost-optimizer
data:
  # Removed image references from ConfigMap as they're now directly in deployment manifests
  ENVIRONMENT: "production"
  LOG_LEVEL: "info"
