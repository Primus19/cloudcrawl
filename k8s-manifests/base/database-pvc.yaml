apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-pvc
  namespace: cloud-cost-optimizer
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 1Gi
