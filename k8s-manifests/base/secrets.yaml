apiVersion: v1
kind: Secret
metadata:
  name: cloud-cost-optimizer-secrets
  namespace: cloud-cost-optimizer
type: Opaque
stringData:
  postgres_user: "postgres"
  postgres_password: "changeme"
  credential_encryption_key: "changeme_with_secure_random_key"
